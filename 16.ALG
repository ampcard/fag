Algoritmo "soma"

Var
   a, b, c:inteiro
   soma: real

Inicio
      escreva("Primeiro valor: ")
      leia(a)
      escreva("Segungo valor: ")
      leia(b)
      escreva("Terceiro valor: ")
      leia(c)
      
      soma <- ((a ^ 2) + (b ^ 2) + (c ^ 2))
      
      escreval()
      escreval("A soma dos quadrados dos números é",soma)

Fimalgoritmo