Algoritmo "areaTriangulo"

Var
   base, altura, area: real

Inicio
      escreva("Base do triângulo em cm: ")
      leia(base)
      escreva("Altura do triângulo em cm: ")
      leia(altura)
      
      area <- (base * altura) / 2

      escreval()
      escreval("A área do triângulo é de", area,"cm²")

Fimalgoritmo